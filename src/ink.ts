/**
 * MindFlow CLI — Ink wrapper types
 * This file intentionally minimal. Actual ink imports come directly from "ink".
 */

// Re-export only what we need, with proper naming
export type { RenderOptions, Instance, Root } from "ink";
