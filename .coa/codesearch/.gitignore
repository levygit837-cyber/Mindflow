# Ignore all local CodeSearch indexes and logs
# These are workspace-specific and can be regenerated by running index_workspace
# Do not commit these to version control

# Ignore everything in this directory
*

# Except this .gitignore file itself
!.gitignore
