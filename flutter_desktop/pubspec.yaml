name: mindflow_desktop
description: MindFlow Desktop - Cross-platform chat interface for AI agents
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_lucide: ^1.0.0
  web_socket_channel: ^3.0.0
  provider: ^6.1.0
  intl: ^0.19.0
  google_fonts: ^6.2.0
  window_manager: ^0.3.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
